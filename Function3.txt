1.原任务添加整理
① 分任务的时间不可以超过总任务的时间(完成)
② 判断是是否总/分任务已经过期，总任务过期进度条换成失败图标，完成换成赞的图标，（总任务过期如何处理？）
分任务，完成写完成字样，过期写过期字样但是仍然可以修改完成，已完成？是任务还在期限之内(完成)

2.新需求整理
① projectList 添加任务提示（没有点击是红色的信封，点击之后是白色的信封，里面有今日正在执行的任务），用户管理图标（改密码，改昵称，改邮箱还是同步邮箱的任务？）
② 用户的原头像处理问题，三种解决方案：第一种，默认值userid.jpg每次上传覆盖（√），第二种上传之后删除原有的图片（写个定时，写入待操作表，半夜执行），第三种就不管了同时存在
3，滞后的需求
给alert改个样式
抬头添加链接方便返回
勋章制度（暂时放一下）
批量增加（放弃）
页面放大和缩小的限制
如何把python加入进去？
数据库中detail多添加一种状态，已超时，如果总项目超时可以显示一些状态，可以有一个能量条。（植物大战僵尸的太阳能量）
总任务下面没有分任务，判断？

总任务的罩层失效
把tomcat抽出来变成常量方便换域名
密码格式判断一下（字幕加数字不少于8位，滞后方便测试）
返回之后验证码，刷新，但是验证的图片没有刷新


