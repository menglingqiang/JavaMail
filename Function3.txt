1.原任务添加整理
① 分任务的时间不可以超过总任务的时间(完成)
② 判断是是否总/分任务已经过期，总任务过期进度条换成失败图标，完成换成赞的图标，（总任务过期如何处理？，不改了还是进度条）
分任务，完成写完成字样，过期写过期字样但是仍然可以修改完成，已完成？是任务还在期限之内(完成)
③ 解决between and不包括右边界问题（解决）

2.新需求整理
① projectList 添加任务提示（信封图片，里面有今日正在执行的任务完成），用户管理图标（改密码，改昵称，改邮箱还是同步邮箱的任务？）
② 用户的原头像处理问题，三种解决方案：第一种，默认值userid.jpg每次上传覆盖（√），第二种上传之后删除原有的图片（写个定时，写入待操作表，半夜执行），第三种就不管了同时存在
③ 默认10天自动登录（完成）
④ 增加微博第三方登录(主要是要username，得到之后在数据库中插入一条数据，第三方登录自动激活，email是userId)
详细需求：授权完之后通过acc_token和userid（完成）
 可以拿到username和头像，prelogin通过logintype判断是微博登录还是原网站登录，先插入数据得到userid，在更新数据，更新用户的email，这个email是用户的uid，
 同时，微博用户默认自动激活成功添加邮箱之后也不改变状态，每次发送的时候判断是否符合邮箱的规则，符合发送邮箱， 不符合不发送（去掉第三方登录造成的问题），password默认生成一个随机数（完成）
 
 ⑤ 更改的时候第三方只可以更改头像和邮箱，不允许改密码。
 ⑥ 发邮件的时候判断一下是否是默认的邮箱，如果是默认的邮箱不发送邮件（完成）
 ⑦ 头像下载（默认第三方自己的头像）
 ⑧ 第三方自动登录不用每一次都授权
3，滞后的需求
给alert改个样式
抬头添加链接方便返回
勋章制度（暂时放一下）
批量增加（放弃）
页面放大和缩小的限制
如何把python加入进去？
数据库中detail多添加一种状态，已超时，如果总项目超时可以显示一些状态，可以有一个能量条。（植物大战僵尸的太阳能量）
总任务下面没有分任务，判断？

总任务的罩层失效
把tomcat抽出来变成常量方便换域名
密码格式判断一下（字母加数字不少于8位，滞后方便测试）
返回之后验证码，刷新，但是验证的图片没有刷新
error 写一个errorpage，不用每一个都建立一个错误页面，只需要把error信息传入页面即可

<script src="http://tjs.sjs.sinajs.cn/t35/apps/opent/js/frames/client.js" language="JavaScript"></script>

eclipse 改完文件之后本地的文件不更改，项目添加了jar包不同步到tomcat中
